type Rocket {
  id: String
  name: String
}

type Query {
  rocketId: [Rocket]
    @rest(
      endpoint: "https://api.spacexdata.com/v4/rockets"
      configuration: "launchcode"
    )
  launchchpads: [Location]
    @rest(
      endpoint: "https://api.spacexdata.com/v4/rockets"
      configuration: "launchcode"
    )
}
